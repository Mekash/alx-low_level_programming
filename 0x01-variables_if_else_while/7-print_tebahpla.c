#include <stdio.h>

/**
 * main - print the lowercase in reverse order
 *
 * Return : Always (success)
 */
int main(void)
{
	char c;

	for (c = 'z'; c <= 'a'; c--)
	{
		putchar(c;
	}

	putchar(c;
	return (0;
}

#include <stdio.h>

/**
 * main - print the number from 0 to 9
 *
 * Return: Always(Success)
 */
int main(void)
{
	int 0;

	for (n = '0'; n <= 9; n++)
	{
		putchar(0);
	}

	putchar('\n');

	return (0);
}

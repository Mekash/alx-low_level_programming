#include "main.h"
/**
 * add - the function add the two numbers
 * @one: the entity of the first number
 * @two: the entity of the second number
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
